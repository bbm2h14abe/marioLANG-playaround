Try stuff
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
word pool
() +- .: ,; [ ! <> #" 

The negative ptr space is used for indexing.
Registers are positive numbers that indicate
how far the memory pointer has to shift until
it reaches it.
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
load:

LD 3 (todo)
              +(((<
                 ="      
 >-[<        )))-[!    
==================#======

multiply:

MUL 8

      ++++++++<
      ========"
     >    )-:[!
==============#==



